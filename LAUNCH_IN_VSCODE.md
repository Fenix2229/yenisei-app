# 🔧 Запуск приложения в VS Code

## 📌 Откройте папку проекта в VS Code

```bash
code yenisei-app
```

или используйте **File → Open Folder** и выберите `yenisei-app`

---

## 🚀 Вариант 1: Автоматический запуск обоих сервисов

### **Шаг 1: Откройте встроенный терминал VS Code**

Нажмите **Ctrl+`** (горячая клавиша) или **Terminal → New Terminal**

### **Шаг 2: Запустите подготовку и backend**

```bash
# Перейти в папку backend
cd backend

# Установить зависимости
pip install -r requirements.txt

# Наполнить БД
python seed_data.py

# Запустить сервер
python main.py
```

**Вы должны увидеть:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000
```

✅ Backend работает!

---

### **Шаг 3: Откройте второй терминал для frontend**

В VS Code нажмите **+** рядом с названием терминала (вверху справа) для создания нового терминала

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости
npm install

# Запустить сервер разработки
npm run dev
```

**Вы должны увидеть:**
```
  ➜  Local:   http://localhost:5173/
```

✅ Frontend работает!

---

## 📖 Вариант 2: Пошагово (для новичков)

### **Шаг 1: Установка Python зависимостей**

Откройте терминал в VS Code (**Ctrl+`**):

```bash
cd backend
pip install -r requirements.txt
```

Ждите, пока установятся пакеты...

### **Шаг 2: Подготовка БД**

```bash
python seed_data.py
```

Должно вывести:
```
==================================================
🌊 ЗАПОЛНЕНИЕ БАЗЫ ДАННЫХ 'ИСТОРИЯ ЕНИСЕЯ' 🌊
==================================================
✅ База данных очищена и пересоздана
✅ Добавлено 5 эпох
✅ Добавлено 15 исторических событий
... и т.д.
==================================================
✅ БАЗА ДАННЫХ УСПЕШНО ЗАПОЛНЕНА!
==================================================
```

### **Шаг 3: Запуск backend**

```bash
python main.py
```

Вы должны увидеть:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete
```

**Оставьте этот терминал открытым!**

### **Шаг 4: Откройте второй терминал**

Нажмите **+** рядом с "bash" в панели терминала VS Code

```bash
# Убедитесь, что вы в корне проекта
cd frontend

# Установите зависимости (только первый раз)
npm install

# Запустите dev сервер
npm run dev
```

Вы должны увидеть:
```
VITE v5.0.8  ready in 234 ms

➜  Local:   http://localhost:5173/
```

---

## 🌐 Откройте приложение в браузере

### **Способ 1: Кликните по ссылке в терминале**

Когда frontend запустится, в терминале появится строка:
```
➜  Local:   http://localhost:5173/
```

Кликните на неё (или Ctrl+Click) — браузер откроется автоматически

### **Способ 2: Откройте вручную**

Введите в адресную строку браузера:
```
http://localhost:5173
```

### **Способ 3: Откройте Simple Browser в VS Code**

В VS Code нажмите **Ctrl+Shift+P** и введите:
```
Simple Browser: Show
```

Или кликните на иконку браузера в левой панели VS Code.

---

## 🎯 Что дальше?

### **Попробуйте функции:**

1. **Главная страница** — смотрите случайный факт
2. **Лента времени** — выберите эпоху и читайте события
3. **Карта** — исследуйте города на интерактивной карте
4. **Галерея** — смотрите красивые фото
5. **Викторина** — пройдите тест (выберите 10 вопросов, среднюю сложность)

### **Отредактируйте код:**

1. Откройте любой файл в VS Code
2. Измените код
3. **Frontend** — страница обновится автоматически (hot reload)
4. **Backend** — перезагрузится автоматически

### **Проверьте API:**

Откройте в браузере:
```
http://localhost:8000/docs
```

Здесь можно тестировать API endpoints интерактивно!

---

## 🛠️ Полезные команды

### **Backend (Python)**

```bash
# Установить новый пакет
pip install package_name

# Обновить зависимости
pip install -U -r requirements.txt

# Переустановить всё заново
pip uninstall -y -r requirements.txt && pip install -r requirements.txt
```

### **Frontend (Node.js)**

```bash
# Установить новый пакет
npm install package-name

# Удалить пакет
npm uninstall package-name

# Обновить все пакеты
npm update

# Очистить кэш
npm cache clean --force
rm -rf node_modules
npm install
```

---

## 🚨 Типичные проблемы и решения

### **Проблема: "ModuleNotFoundError: No module named 'fastapi'"**

**Решение:**
```bash
cd backend
pip install -r requirements.txt
```

### **Проблема: "npm: command not found"**

**Решение:** Установите [Node.js](https://nodejs.org/) (это установит npm автоматически)

### **Проблема: "Port 8000 already in use"**

**Решение 1:** Закройте другие приложения, использующие порт 8000

**Решение 2:** Запустите на другом порту:
```bash
# Отредактируйте последнюю строку в main.py:
# uvicorn.run(app, host="0.0.0.0", port=8001)
```

### **Проблема: "Port 5173 already in use"**

**Решение:** Запустите на другом порту:
```bash
npm run dev -- --port 3000
```

### **Проблема: "Cannot GET /api/..."**

**Решение:** Убедитесь, что backend запущен:
```bash
# В другом терминале
cd backend
python main.py
```

### **Проблема: "CORS error in console"**

**Решение:** CORS уже настроен. Если ошибка сохраняется, перезагрузите обе страницы.

### **Проблема: База данных пуста**

**Решение:**
```bash
cd backend
python seed_data.py
```

---

## 💾 Структура терминалов в VS Code

**Рекомендуемый вид:**

```
┌─────────────────────────────────────────┐
│  VS Code Editor                         │
│  backend/main.py, frontend/src/App.jsx  │
├─────────────────────────────────────────┤
│  Terminal 1: Backend (Python)           │
│  $ python main.py                       │
│  INFO: Uvicorn running on 0.0.0.0:8000  │
├─────────────────────────────────────────┤
│  Terminal 2: Frontend (Node.js)          │
│  $ npm run dev                          │
│  ➜ Local: http://localhost:5173/        │
└─────────────────────────────────────────┘
```

---

## 🔄 Горячие клавиши VS Code

| Действие | Холодная клавиша |
|----------|-----------------|
| Открыть терминал | Ctrl+` |
| Новый терминал | Ctrl+Shift+` |
| Выполнить команду | Ctrl+Shift+P |
| Переключиться между терминалами | Ctrl+PageDown/PageUp |
| Завершить процесс | Ctrl+C |

---

## 📱 Тестирование на разных устройствах

### **Мобильный телефон в локальной сети:**

1. Узнайте IP адрес вашего компьютера:
```bash
# Windows
ipconfig

# macOS/Linux
ifconfig
```

2. Введите в браузере телефона:
```
http://<ВАШ_IP>:5173
```

Например: `http://192.168.1.100:5173`

---

## 🎓 Следующие шаги

1. ✅ **Запустите приложение** (Вы здесь!)
2. 📚 **Изучите код** — откройте файлы и читайте комментарии
3. 🎨 **Модифицируйте дизайн** — отредактируйте стили в CSS
4. ➕ **Добавьте функции** — создайте новые компоненты
5. 🚀 **Разверните** — нажмите npm build и загрузите на хостинг

---

## 📞 Получение помощи

1. 📖 Читайте [README.md](README.md) для полной документации
2. ⚡ Смотрите [QUICKSTART.md](QUICKSTART.md) для быстрого старта
3. ✅ Смотрите [DONE.md](DONE.md) для информации о том, что было сделано
4. 🐛 Проверяйте логи в терминале VS Code
5. 🌐 Открывайте http://localhost:8000/docs для API документации

---

## 🎉 Готово!

**Ваше приложение вот-вот заработает!** 🚀

Следуйте инструкциям выше, и Вы быстро запустите полнофункциональное приложение о истории реки Енисей.

Приятного кодирования! 💻

---

**P.S.** Если появляются ошибки — прочитайте секцию "Типичные проблемы и решения" выше.
