[build]
  base = "frontend"
  command = "npm ci && npx vite build --outDir docs"
  publish = "docs"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
